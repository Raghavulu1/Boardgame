pipeline {
    agent any
    
    tools {
        maven 'maven3'
        jdk 'jdk17'
    }

    stages {     
        stage('New-JenkinsFile') {
            steps {
                echo "New-JenkinsFile"
            }
        }    


        stage('Compile') {
            steps {
               sh "mvn compile"
            }
        }
        
        stage('Test') {
            steps {
                sh "mvn test"
            }
        }
        
        stage('Build') {
            steps {
                sh "mvn package"
            }
        }

        stage('Hello World') {
            steps {
                echo "Hello World"
            }
        }
    }
}
